# 🐾 GrosiDog — Каталог собак питомника

> Учебный проект на Vue 3 + Vuex 4 без сборщика(CDN).  
> Каталог породистых собак питомника GrosiDog с фильтрацией, карточками и модальными окнами.

---

## ✨ Возможности

- 📋 **Каталог 13 собак** четырёх пород (немецкая овчарка, корги, шпиц, джек-рассел)
- 🔍 **Фильтрация по породе** с мгновенным обновлением списка
- 🪟 **Модальное окно** с подробной информацией о собаке
- 📸 **Лёгкая замена фото** — достаточно поменять одну строку в `store.js`
- 🏷️ **Бейдж «Продаётся»** для доступных к продаже собак
- 📱 **Адаптивная вёрстка** — работает на мобильном и десктопе
- ✅ **Без сборщика** — открывается напрямую в браузере

---

## 📁 Структура проекта

```
grosidog/
├── index.html        # Точка входа, подключение CDN
├── style.css         # Все стили
├── js/
    ├── store.js      # Vuex: состояние, геттеры, мутации, экшены
    └── app.js        # Vue-компоненты приложения
└── images/
    ├── shepherd-ares.jpg    # (добавьте свои фото)
```

---

## 🚀 Запуск

Сборщик не нужен - проект работает напрямую в браузере.

### Вариант 1 - Live Server (рекомендуется)

1. Установите расширение **Live Server** в VS Code
2. Откройте папку проекта в VS Code
3. Нажмите правой кнопкой на `index.html` → **Open with Live Server**

### Вариант 2 - открыть файл напрямую

Просто откройте `index.html` двойным кликом.  
> ⚠️ При таком способе свои фото из папки `images/` могут не загружаться из-за ограничений браузера — лучше используйте Live Server.

---

## 🏗️ Архитектура Vuex

```
store.js
├── state
│   ├── dogs[]         — массив всех собак питомника
│   ├── activeFilter   — текущий фильтр ('all' | 'shepherd' | 'corgi' | 'spitz' | 'jack')
│   └── selectedDog    — собака, открытая в модальном окне (null = закрыто)
│
├── getters
│   ├── filteredDogs   — отфильтрованный список собак
│   ├── breedCounts    — количество собак по каждой породе
│   ├── activeFilter   — текущий фильтр (для подсветки кнопки)
│   └── selectedDog    — выбранная собака (для модалки)
│
├── mutations
│   ├── SET_FILTER     — установить породный фильтр
│   ├── SELECT_DOG     — открыть модалку с собакой
│   └── CLOSE_DOG      — закрыть модалку
│
└── actions
    ├── setFilter      — вызывает SET_FILTER
    ├── selectDog      — вызывает SELECT_DOG
    └── closeDog       — вызывает CLOSE_DOG
```

В компонентах используются хелперы Options API: `mapGetters` и `mapActions`.

---

## 🧩 Компоненты

| Компонент | Файл | Описание |
|---|---|---|
| `AppHeader` | `app.js` | Шапка с логотипом питомника |
| `BreedStats` | `app.js` | Бейджи с количеством собак по породам |
| `BreedFilter` | `app.js` | Кнопки фильтрации по породе |
| `DogCard` | `app.js` | Карточка одной собаки |
| `DogModal` | `app.js` | Модальное окно с деталями |
| `App` (root) | `app.js` | Корневой компонент, собирает всё вместе |

---

## 🐶 Собаки питомника

| # | Кличка | Порода | Пол | Возраст |
|---|---|---|---|---|
| 1 | Арес | Немецкая овчарка | ♂ | 3 года |
| 2 | Белла | Немецкая овчарка | ♀ | 2 года |
| 3 | Граф | Немецкая овчарка | ♂ | 5 лет |
| 4 | Дара | Немецкая овчарка | ♀ | 1 год |
| 5 | Зевс | Немецкая овчарка | ♂ | 4 года |
| 6 | Пончик | Корги | ♂ | 2 года |
| 7 | Рыжик | Корги | ♀ | 3 года |
| 8 | Снежка | Корги | ♀ | 1 год |
| 9 | Жемчуг | Шпиц | ♀ | 2 года |
| 10 | Терри | Джек-рассел | ♂ | 2 года |
| 11 | Молния | Джек-рассел | ♀ | 1 год |
| 12 | Буян | Джек-рассел | ♂ | 3 года |
| 13 | Пеппер | Джек-рассел | ♀ | 2 года |

